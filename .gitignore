# --- Design document: intentionally NEVER committed (internal planning only) ---
DESIGN.typ
design.typ
*.typ
*.pdf

# --- Rust / Cargo ---
/target/
**/target/
Cargo.lock.orig
*.rs.bk

# --- Secrets / API keys (never commit) ---
.env
.env.*
*.key
secrets.toml
config.local.toml

# --- Native build intermediates (C++/SWELL shim) ---
*.obj
*.o
*.lib
*.exp
*.pdb
*.ilk
*.dll
*.dylib
*.so

# --- Build logs ---
*.log

# --- OS / editor cruft ---
.DS_Store
Thumbs.db
.vs/
.vscode/
.idea/
*.swp
*~

# SWELL/WDL checkout (cloned fresh in CI; never committed)
/vendor/

# SWELL dialog/menu tables generated from assistant.rc by swell_resgen.php (build.rs)
/cpp/assistant.rc_mac_dlg
/cpp/assistant.rc_mac_menu

# macOS code-signing / notarization secret material — NEVER commit these.
# Base64 them into GitHub Actions secrets (see docs/macos-notarization.md), then
# delete the local copies. Committing a .p12/.p8/private key = a compromised
# signing identity (revoke + reissue).
*.p12
*.p8
*.cer
*.csr
*.pem
*.b64
